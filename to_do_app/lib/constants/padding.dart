import 'package:flutter/material.dart';

const allPadding = EdgeInsets.all(10.0);
const onlyBottomPadding = EdgeInsets.only(bottom: 10.0);
const onlyLeftPadding = EdgeInsets.only(left: 10.0);
const onlyTopPadding = EdgeInsets.only(top: 10.0);
const onlyRightPadding = EdgeInsets.only(right: 10.0);
const horizontalPadding = EdgeInsets.symmetric(horizontal: 20.0);
const verticalPadding = EdgeInsets.symmetric(vertical: 15.0);
